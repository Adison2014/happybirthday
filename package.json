{
  "name": "birthday_web-main",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "dev": "browser-sync start --proxy 'localhost:3000' --files 'public/**/*.*' --no-notify --no-open & nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "express": "^4.21.1",
    "nodemailer": "^6.9.15"
  }
}
